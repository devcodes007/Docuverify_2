# Production API runtime for Render.
# Training/dev dependencies stay in requirements-train.txt and requirements-dev.txt.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
httpx>=0.27

rank_bm25>=0.2.2
qdrant-client>=1.12
numpy>=1.26

beautifulsoup4>=4.12
markdown>=3.6
pypdf>=5.0
python-multipart>=0.0.9

# Keep transformer inference available for GROUNDEDNESS_MODEL when a trained
# classifier artifact is deployed. Render Free may still exceed 512 MB with
# this enabled; see MEMORY_OPTIMIZATION.md.
torch>=2.2
transformers>=4.42
